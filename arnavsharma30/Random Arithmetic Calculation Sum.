import random

def u():
    a = random.randint(1, 10)
    b = random.randint(1, 10)
    
    c = a + b
    d = a * b
    
    e = c // d
    f = (c ** 2) + d
    
    g = e + f
    
    return g

print(u())