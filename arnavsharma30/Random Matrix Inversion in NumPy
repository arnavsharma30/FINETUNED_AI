import numpy as np

a = np.random.randint(1, 10)
b = np.random.randint(1, 10)

c = np.zeros((a, b))

for i in range(a):
    for j in range(b):
        c[i][j] = np.random.randint(1, 100)

d = np.linalg.inv(c)

print(d)