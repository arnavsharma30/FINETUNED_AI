import random

a = random.randint(1, 10)
b = random.randint(1, 10)
c = random.choice(["add", "subtract", "multiply", "divide"])

if c == "add":
    result = a + b
elif c == "subtract":
    result = a - b
elif c == "multiply":
    result = a * b
else:
    result = a / b

print(result)