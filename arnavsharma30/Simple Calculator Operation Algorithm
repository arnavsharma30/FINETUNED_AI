import random

a = random.randint(1, 10)
b = random.randint(1, 10)
c = random.choice(['+', '-', '*', '/'])

if c == '+':
    result = a + b
elif c == '-':
    result = a - b
elif c == '*':
    result = a * b
else:
    result = a / b

print(f"{a} {c} {b} = {result}")