import random

def u(x, y):
    z = x * y
    a = x + y
    b = x - y
    c = x / y
    d = x % y
    e = x ** y
    
    return z, a, b, c, d, e

random_num1 = random.randint(1, 10)
random_num2 = random.randint(1, 10)

result = u(random_num1, random_num2)

print(result)