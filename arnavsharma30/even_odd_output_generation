import random

# generate a random number between 1 and 100
x = random.randint(1, 100)

# check if the number is even or odd
if x % 2 == 0:
    y = x * 2
else:
    y = x * 3

# output the result
print(y)