import random

def u():
    r = random.randint(1, 10)
    s = random.randint(1, 10)
    t = random.choice(['a', 'b', 'c'])

    if t == 'a':
        return r + s
    elif t == 'b':
        return r - s
    else:
        return r * s

print(u())