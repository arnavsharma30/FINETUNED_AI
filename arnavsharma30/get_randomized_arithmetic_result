import random

def generate_unique_code():
    x = random.randint(1, 10)
    y = random.randint(1, 10)
    
    if x > y:
        z = x * y
    elif x < y:
        z = x + y
    else:
        z = x - y
        
    return z

result = generate_unique_code()
print(result)