import random

def unique_code():
    x = random.randint(1, 10)
    y = random.randint(1, 10)

    if x % 2 == 0:
        z = x * y
    else:
        z = x + y

    return z

result = unique_code()
print(result)