import numpy as np

def rnd_py_code():
    a = np.random.randint(1, 10)
    b = np.random.randint(1, 10)

    c = a * b

    for i in range(c):
        if i % 2 == 0:
            c /= 2
        else:
            c += i

    return c

result = rnd_py_code()
print(result)